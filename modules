# Three different line formats are valid:
#	key	-a    aliases...
#	key [options] directory
#	key [options] directory files...
#
# Where "options" are composed of:
#	-i prog		Run "prog" on "cvs commit" from top-level of module.
#	-o prog		Run "prog" on "cvs checkout" of module.
#	-e prog		Run "prog" on "cvs export" of module.
#	-t prog		Run "prog" on "cvs rtag" of module.
#	-u prog		Run "prog" on "cvs update" of module.
#	-d dir		Place module in directory "dir" instead of module name.
#	-l		Top-level directory only -- do not recurse.
#
# NOTE:  If you change any of the "Run" options above, you'll have to
# release and re-checkout any working directories of these modules.
#
# And "directory" is a path to a directory relative to $CVSROOT.
#
# The "-a" option specifies an alias.  An alias is interpreted as if
# everything on the right of the "-a" had been typed on the command line.
#
# You can encode a module within a module by using the special '&'
# character to interpose another module into the current module.  This
# can be useful for creating a module that consists of many directories
# spread out over the entire source repository.
libcontain-config -d libcontain config
libcontain-arch -d libcontain/arch arch
libcontain-libreplace -d libcontain/libreplace libreplace
libcontain-libmemory -d libcontain/libmemory libmemory
libcontain-libcontain -d libcontain/libcontain libcontain
libcontain-libthread -d libcontain/libthread libthread
release-libcontain -a libcontain-config libcontain-libcontain libcontain-arch libcontain-libreplace libcontain-libmemory

libmemory-arch -d libmemory/arch arch
libmemory-libmemory -d libmemory/libmemory libmemory
libmemory-config -d libmemory config
release-libmemory -a libmemory-config libmemory-libmemory libmemory-arch

preprocessor-preprocessor -d preprocessor/preprocessor preprocessor
preprocessor-config -d preprocessor config
preprocessor-arch -d preprocessor/arch arch
preprocessor-libcontain -d preprocessor/libcontain libcontain
preprocessor-libreplace -d preprocessor/libreplace libreplace
preprocessor-libconf -d preprocessor/libconf libconf
preprocessor-libmemory -d preprocessor/libmemory libmemory
release-preprocessor -a preprocessor-config preprocessor-preprocessor preprocessor-arch preprocessor-libcontain preprocessor-libreplace preprocessor-libconf preprocessor-libmemory 

libconf-config -d libconf config
libconf-arch -d libconf/arch arch
libconf-libconf -d libconf/libconf libconf
libconf-libmemory -d libconf/libmemory libmemory
libconf-libcontain -d libconf/libcontain libcontain
libconf-libreplace -d libconf/libreplace libreplace
release-libconf -a libconf-config libconf-libconf libconf-libreplace libconf-libcontain libconf-arch libconf-libmemory

libthread-config -d libthread config
libthread-libthread -d libthread/libthread libthread
libthread-arch -d libthread/arch arch
libthread-libreplace -d libthread/libreplace libreplace
libthread-libmemory -d libthread/libmemory libmemory
release-libthread -a libthread-config libthread-libthread libthread-arch libthread-libmemory libthread-libreplace

